# Credentials are loaded from .env (see backend/Cinebuzz/.env.example).
# Copy .env.example to .env and set values. Optional: keep this file empty or use only for non-secret overrides.
# Spring Boot optional import removed — use .env only for secrets.
